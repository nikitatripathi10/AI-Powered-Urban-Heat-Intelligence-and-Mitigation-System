# Set to the full backend origin when running the frontend standalone
# against a remote backend (e.g. http://localhost:8000).
# Leave empty when Vite's dev proxy or nginx handles /api routing.
VITE_API_URL=
